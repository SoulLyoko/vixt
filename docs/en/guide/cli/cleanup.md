# vixt cleanup

Remove files and build caches generated by Vixt.

## Usage

```sh
vixt cleanup [root]
```

The command removes the following directories from the project root:

- `.vixt`
- `.output`
- `dist`
- `node_modules/.vite`
- `node_modules/.cache`
